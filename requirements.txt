############################################################
# Requirements principais do projeto
# Cada linha aponta para a ficha em docs/libs/*.md
# Mantidos pins para reprodutibilidade; atualizar somente após smoke
############################################################

robotframework==7.3.2                       # Core framework (docs/libs/robotframework.md)
robotframework-browser==19.0.0              # Web UI Playwright (docs/libs/browser.md)  # Usa wheels com grpc para Py3.13
robotframework-requests==0.9.6              # HTTP keywords (docs/libs/requestslibrary.md)
protobuf==5.28.3                            # Protocol Buffers
python-dotenv==1.0.1                        # Env loader 
requests==2.32.3                            # Underlying HTTP client
robotframework-jsonschemalibrary==1.0       # Validação de respostas via JSON Schema
robotframework-robocop==6.6.1               # Análise estática de código
pyodbc==5.2.0                             # ODBC database connector

# gRPC toolchain (opcional): mover para arquivo separado para evitar falhas de build
# Instale quando precisar testar gRPC e houver toolchain C++ no SO (gcc/g++)


